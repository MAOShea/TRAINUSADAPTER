Create an Übersicht widget that displays Docker container information. The widget should execute the 'docker ps -a' command every second to get real-time container status, parse the output to extract container ID, image, command, ports, and status information, display the data in a clean table format with Docker icon in the header, handle error states when Docker daemon is stopped, show "No Docker containers running" when no containers are present, and use a dark semi-transparent background with blue accent colors, PT Mono font, and proper table styling with rounded corners.
